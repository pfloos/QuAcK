# Basis Set directory

To get many common basis sets from the Basis Set Exchange Website (https://www.basissetexchange.org/) one can install the dedicated tool with pip:

`pip install basis-set-exchange`

Then executing the script here with python3 makes the basis available for QuAcK:

`python3 install.py`
